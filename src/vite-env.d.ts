/// <reference types="vite/client" />

interface ImportMetaEnv {
  /** Base URL for optional external sequence/record API (e.g. canvas / lab tools). */
  readonly VITE_EXTERNAL_RECORDS_API?: string;
}
